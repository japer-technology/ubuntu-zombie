#!/usr/bin/env bash
#
# beep-agents — Beep's closed family-manager CLI.

set -Eeuo pipefail

if (( EUID != 0 )); then
  echo "beep-agents must run as root (try: sudo $0)" >&2
  exit 73
fi

BEEP_DIR="${BEEP_DIR:-/opt/beep}"
exec /usr/bin/python3 "${BEEP_DIR}/agent/family.py" "$@"
